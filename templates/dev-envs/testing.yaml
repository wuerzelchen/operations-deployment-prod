apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devteam-testing
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: "https://kubernetes.default.svc"
  project: default
  source:
    path: .
    repoURL: "https://github.com/wuerzelchen/project-deployment-dev"
    targetRevision: testing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true